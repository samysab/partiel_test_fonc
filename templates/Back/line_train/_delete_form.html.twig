<form method="post" action="{{ path('admin_line_train_delete', {'id': line_train.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ line_train.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('train_delete', {'id': train.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ train.id) }}">
    <button class="btn">Delete</button>
</form>

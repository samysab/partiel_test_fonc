{% extends 'base.html.twig' %}

{% block title %}Train{% endblock %}

{% block body %}
    <h1>Train</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ train.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ train.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ train.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('train_index') }}">back to list</a>

    <a href="{{ path('train_edit', {'id': train.id}) }}">edit</a>

    {{ include('Front/train/_delete_form.html.twig') }}
{% endblock %}

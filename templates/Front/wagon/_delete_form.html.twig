<form method="post" action="{{ path('wagon_delete', {'id': wagon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wagon.id) }}">
    <button class="btn">Delete</button>
</form>
